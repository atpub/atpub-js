{
  "name": "atpub-js",
  "private": true,
  "scripts": {
    "build": "pnpm run --sort --recursive --stream '/^(build|build:.+)$/'"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "pnpm": {
	  "onlyBuiltDependencies": [
		  "esbuild"
	  ]
  }
}
